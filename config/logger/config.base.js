const config = module.exports = {};
config.transport = {
    console: true,
    fluentd: false,
    logstash: false,
    file: false
};

config.extraDetails = false;
config.isDefault = true;
config.verbosityLevel = 0;
